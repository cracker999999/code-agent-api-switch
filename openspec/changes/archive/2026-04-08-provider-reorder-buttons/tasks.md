## 1. 数据层重排能力

- [x] 1.1 在 `DatabaseService` 新增同 ToolType 内相邻交换 `SortOrder` 的重排方法（上移/下移）
- [x] 1.2 在重排方法中使用事务保证两条记录交换原子性
- [x] 1.3 处理边界项（第一项上移、最后一项下移）为无操作返回

## 2. 主窗口按钮与交互

- [x] 2.1 在 `MainWindow.xaml` 卡片操作区新增上移 `↑` 与下移 `↓` 按钮
- [x] 2.2 在 `MainWindow.xaml.cs` 实现上移按钮点击事件：调用数据层重排并刷新列表
- [x] 2.3 在 `MainWindow.xaml.cs` 实现下移按钮点击事件：调用数据层重排并刷新列表
- [x] 2.4 实现边界禁用逻辑：首项禁用上移、末项禁用下移

## 3. 回归与验证

- [x] 3.1 构建验证：`dotnet restore` + `dotnet build`
- [x] 3.2 手工验证：Codex 列表上移/下移顺序立即变化且持久化
- [x] 3.3 手工验证：Claude Code 列表重排不影响 Codex 列表
- [x] 3.4 手工验证：重启后顺序保持，边界按钮禁用状态正确


